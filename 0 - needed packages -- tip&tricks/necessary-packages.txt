https://github.com/alperakcan/fuse-ext2 (needs to be compiled on the system)

To fix compilation issues on opensuse:
in the config.ac file add "AM_PROG_CC_C_O"
then 
$ ./autogen.sh
$ ./configure
$ make
$ sudo make install




https://github.com/hak86/android-simg2img
(download and do "make" to compile simg2img, then put the bin in the user bin folder)






-- 

put the fuse-ext2 bin in the distro user bin folder

run the extract script as su (root)
